version: 1.0
runtime: nodejs18
build:
  commands:    
    build:
      - npm install --production
      - cd my-app && npm install && npm run-script build                                  
run:                              
  command: node index.js